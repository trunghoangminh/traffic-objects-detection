# Contains all constant of application
class AppConstants:
    # Use for load and save model
    ROOT_MODEL = "../../models/"

    MODEL_PATH_NAME = ROOT_MODEL + "training-result/traffic-object-detection.tflearn"
    IMAGE_CLASSES = ROOT_MODEL + "training-result/image-classes.txt"

    DATA_SET_CARS = ROOT_MODEL + "car"
    DATA_SET_MOTOBIKES = ROOT_MODEL + "motobike"
